{
  "name": "codetoclick",
  "version": "1.6.0",
  "private": true,
  "type": "module",
  "description": "Code to Click - Profitability Intelligence",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  },
  "scripts": {
    "db:status": "node scripts/migrate.mjs --status",
    "db:migrate": "node scripts/migrate.mjs",
    "build": "sh src/build.sh && cp src/app.html public/index.html && rm -f src/app.html src/test-page.html"
  },
  "devDependencies": {
    "pg": "^8.13.1"
  }
}
